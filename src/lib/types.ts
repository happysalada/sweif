export enum FormState {
	Ready,
	Submitted,
	Success,
	Error,
}

export enum FiatCurrency {
  COP = 'COP',
  EUR = 'EUR',
}

export enum StableCoin {
  EURC = 'EURC',
  USDC = 'USDC',
  USDT = 'USDT',
}
